Token is not stored in localStorage, but required for fetch()

/newpost/page.js -> How to access Post -> title, content and slug which is used in JSON.stringify() ?

In the routes which have the validSession middleware added, update the middleware function to receive the userId and required user data from validSession

Login form -> should redirect to user home page 

User home page should have - create newpost, list all post, edit post, delete post
