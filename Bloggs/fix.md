
In the routes which have the validSession middleware added, update the middleware function to receive the userId and required user data from validSession

User home page should have - create newpost, edit post, delete post

NewPost -> same post which is saved as status - 'Draft' is creating a new record in DB when status changes to 'Published'. Only post status should change, a new record should not get created.

Update Post Component

Delete Post component
